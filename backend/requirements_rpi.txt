# Flask Backend Dependencies - Raspberry Pi 5 Optimized

# Web framework
Flask>=2.3.0
flask-cors>=4.0.0
flask-jwt-extended>=4.5.0
flask-sqlalchemy>=3.0.0

# Database
SQLAlchemy>=2.0.0

# Security
Werkzeug>=2.3.0

# PDF generation
reportlab>=4.0.0

# Audio recording - ARM-compatible version
# Note: Install system package first: sudo apt install python3-pyaudio portaudio19-dev
pyaudio>=0.2.11

# Speech recognition - Vosk (ARM-compatible)
vosk>=0.3.45

# HTTP requests (for OpenRouter API)
requests>=2.31.0

# Configuration
PyYAML>=6.0

# Environment variables (optional, for .env file support)
python-dotenv>=1.0.0

# Production server
gunicorn>=21.2.0

# Audio processing (for converting uploaded audio)
pydub>=0.25.1

# Note: Also install ffmpeg system package:
# sudo apt install ffmpeg

# Note: NLTK, scikit-learn, numpy removed - using OpenRouter API for summarization
# No local NLP processing needed
